import * as bitcoin from 'bitcoinjs-lib';
import { TNetwork, TDecodeRawTx } from './types';
import { Result } from './helpers/result';
declare const decodeRawTx: (rawTx: string, _network?: TNetwork) => Result<TDecodeRawTx>;
export type PaymentFn = (a: bitcoin.Payment, opts?: bitcoin.PaymentOpts) => bitcoin.Payment;
export default decodeRawTx;
