import { MessageRouter } from '../structs/MessageRouter.mjs';
import { Router } from '../structs/Router.mjs';
import { CommonBase } from './CommonBase.mjs';
import * as bindings from '../bindings.mjs';
/**
 * A [`Router`] implemented using [`find_route`].
 *
 * # Privacy
 *
 * Implements [`MessageRouter`] by delegating to [`DefaultMessageRouter`]. See those docs for
 * privacy implications.
 */
export class DefaultRouter extends CommonBase {
    /* @internal */
    constructor(_dummy, ptr) {
        super(ptr, bindings.DefaultRouter_free);
    }
    /**
     * Creates a new router.
     */
    static constructor_new(network_graph, logger, entropy_source, scorer, score_params) {
        const ret = bindings.DefaultRouter_new(CommonBase.get_ptr_of(network_graph), CommonBase.get_ptr_of(logger), CommonBase.get_ptr_of(entropy_source), CommonBase.get_ptr_of(scorer), CommonBase.get_ptr_of(score_params));
        const ret_hu_conv = new DefaultRouter(null, ret);
        CommonBase.add_ref_from(ret_hu_conv, ret_hu_conv);
        CommonBase.add_ref_from(ret_hu_conv, network_graph);
        CommonBase.add_ref_from(ret_hu_conv, logger);
        CommonBase.add_ref_from(ret_hu_conv, entropy_source);
        CommonBase.add_ref_from(ret_hu_conv, scorer);
        return ret_hu_conv;
    }
    /**
     * Constructs a new Router which calls the relevant methods on this_arg.
     * This copies the `inner` pointer in this_arg and thus the returned Router must be freed before this_arg is
     */
    as_Router() {
        const ret = bindings.DefaultRouter_as_Router(this.ptr);
        const ret_hu_conv = new Router(null, ret);
        CommonBase.add_ref_from(ret_hu_conv, this);
        return ret_hu_conv;
    }
    /**
     * Constructs a new MessageRouter which calls the relevant methods on this_arg.
     * This copies the `inner` pointer in this_arg and thus the returned MessageRouter must be freed before this_arg is
     */
    as_MessageRouter() {
        const ret = bindings.DefaultRouter_as_MessageRouter(this.ptr);
        const ret_hu_conv = new MessageRouter(null, ret);
        CommonBase.add_ref_from(ret_hu_conv, this);
        return ret_hu_conv;
    }
}
//# sourceMappingURL=DefaultRouter.mjs.map