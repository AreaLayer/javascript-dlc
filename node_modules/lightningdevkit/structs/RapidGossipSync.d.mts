import { Option_u64Z } from '../structs/Option_u64Z.mjs';
import { Logger } from '../structs/Logger.mjs';
import { NetworkGraph } from '../structs/NetworkGraph.mjs';
import { Result_u32GraphSyncErrorZ } from '../structs/Result_u32GraphSyncErrorZ.mjs';
import { CommonBase } from './CommonBase.mjs';
/**
 * The main Rapid Gossip Sync object.
 *
 * See [crate-level documentation] for usage.
 *
 * [crate-level documentation]: crate
 */
export declare class RapidGossipSync extends CommonBase {
    /**
     * Instantiate a new [`RapidGossipSync`] instance.
     */
    static constructor_new(network_graph: NetworkGraph, logger: Logger): RapidGossipSync;
    /**
     * Update network graph from binary data.
     * Returns the last sync timestamp to be used the next time rapid sync data is queried.
     *
     * `update_data`: `&[u8]` binary stream that comprises the update data
     * `current_time_unix`: `Option<u64>` optional current timestamp to verify data age
     */
    update_network_graph_no_std(update_data: Uint8Array, current_time_unix: Option_u64Z): Result_u32GraphSyncErrorZ;
    /**
     * Returns whether a rapid gossip sync has completed at least once.
     */
    is_initial_sync_complete(): boolean;
}
